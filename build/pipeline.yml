name: $(Date:yyyyMMdd)$(Rev:.r)

trigger:
  branches:
    include:
      - main
  tags:
    include: ['*']
pr: none

resources:
  repositories:
    - repository: templates
      type: github
      name: microsoft/vscode-engineering
      ref: main
      endpoint: Monaco

extends:
  template: azure-pipelines/vscode-extension.yml@templates
  parameters:
    buildSteps:
      - script: npm ci
        displayName: Install dependencies

      - script: npm run vscode:prepublish
        displayName: Compile
